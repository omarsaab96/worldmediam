{
    "name": "worldMediaManagement",
    "version": "1.0.0",
    "engines": {
        "node": ">=18 <19"
    },
    "scripts": {
        "start": "cd backend && node server.js",
        "deployBackend": "cd backend && npm run build",
        "deployFrontend":"cd frontend && npm install --legacy-peer-deps && ng build",
        "deploy":"npm run deployFrontend && npm run deployBackend"
    }
}